package com.senai.experience.config;

/**
 * Classe base para testes de integração.
 * Usa H2 em memória (configurado em application-test.properties) — sem Docker necessário.
 */
public abstract class PostgresTestContainer {
}
